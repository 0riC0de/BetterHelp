# Database Maintenance Feature

This administrator-only package displays database counts and invokes fixed,
allowlisted maintenance operations. It never accepts SQL, table names, or model
names. Destructive actions require an exact typed confirmation and preserve all
technician credentials and active administrator sessions.
