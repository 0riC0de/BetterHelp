# Inventory Feature

This package owns machine and department models, inventory data access, Wake-on-LAN
actions, and reusable inventory components. Route files own page composition;
components do not call the backend directly.
