# Technicians Feature

This package owns administrator-facing technician models, hooks, and reusable
forms. Password resets revoke active sessions. Route files own authorization and
page composition; components contain presentation and form state only.
