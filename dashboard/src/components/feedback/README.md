# Feedback Components

This package owns reusable loading and empty-state presentation. `LoadingSkeleton`
uses a small variant/row/height API so feature packages do not create dedicated
skeleton components. It contains no data fetching or feature-specific behavior.
