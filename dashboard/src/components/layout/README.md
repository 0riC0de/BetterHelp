# Layout Components

This package owns authenticated application chrome: session gating, the header,
and the navigation drawer. Route implementations live in `app`, while this package
contains only reusable layout behavior and imports no feature page.
